Summary counters under the dashboard highlights. Two per row at 47.5% width, 12px gap.

```jsx
<StatCard label="Atleti Seguiti" value={18} icon="people-outline" color="#2563EB" />
<StatCard label="Allenamenti" value={7} icon="barbell-outline" color="#10B981" />
```

Pass a hex `color`: the icon chip background is that hex at 8% (`#2563EB14`).
